# Example secrets for sops-nix (plaintext example; encrypt before committing)
# After editing, encrypt with:
#   sops --age AGE_PUBLIC_KEY --encrypt --in-place secrets/home.yaml
#
# Replace the values below with real secrets, then encrypt.

cloudflare_token: "<your-cloudflare-token>"
# kubeconfig: |
#   apiVersion: v1
#   clusters: [...]
#   contexts: [...]
#   current-context: ...
#   users: [...]
